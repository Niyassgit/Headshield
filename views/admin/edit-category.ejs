<%- include("../partials/admin/header") %>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <!-- Card for styling -->
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white text-center">
                    <h4>Edit Category</h4>
                </div>
                <div class="card-body">
                    <form method="POST" id="editCategoryForm" action="/admin/editCategory/<%= category._id %>" onsubmit="return validateForm(event)">
                        <!-- Category Name -->
                        <div class="form-group mb-4">
                            <label for="categoryName" class="form-label font-weight-bold">Category Name</label>
                            <input 
                                type="text" 
                                id="categoryName" 
                                name="categoryName" 
                                value="<%= category.name %>" 
                                class="form-control border rounded-pill" 
                                placeholder="Enter category name" 
                                required
                            />
                            <div id="name-error" class="error-message text-danger mt-1"></div>
                        </div>

                        <!-- Category Description -->
                        <div class="form-group mb-4">
                            <label for="descriptionId" class="form-label font-weight-bold">Description</label>
                            <textarea 
                                id="descriptionId" 
                                name="description" 
                                class="form-control border rounded" 
                                rows="4" 
                                placeholder="Enter category description" 
                                required
                            ><%= category.description %></textarea>
                            <div id="description-error" class="error-message text-danger mt-1"></div>
                        </div>

                        <!-- Submit Button -->
                        <div class="form-group text-end">
                            <button type="submit" class="btn btn-primary px-4">Update Category</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include("../partials/admin/footer") %>